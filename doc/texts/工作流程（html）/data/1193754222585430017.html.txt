一、功能背景
原先触发子流程只能是到达节点或者离开节点后立即触发，但有些场景下，子流程的业务不是立马就要做的，需要根据表单上的日期时间去做触发的判断依据或者延迟创建出子流程，比如到达节点多少时间后触发等，需支持延迟触发及按日期字段触发，这样能灵活的结合实际业务场景控制子流程的创建时间。
二、功能详细说明
主流程触发设置中，触发时间除了立即触发，还可以设置延迟触发，设定固定时间或者根据表单字段获取时间，如下图所示