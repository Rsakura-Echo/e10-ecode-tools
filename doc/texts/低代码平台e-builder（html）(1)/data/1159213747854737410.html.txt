一、
概述
在e10多租户办公环境、协同场景中，各租户之间也会存在各类业务数据、业务动作交换传输的使用场景。数据交换中心是承载打通各租户之间信息交互壁垒的平台，交换中心通过自定义交换模板，并支持各租户数据发送、接收方通过esb动作流组件通过交换中心与其他各租户进行数据交互，以满足支持不同租户之间存在各类业务数据、业务动作交换的使用场景。
类型
组件名称
描述
执行组件
发送交换数据
用于数据发送方发送交换数据
执行组件
反馈信息
数据交换发送方、接收方对已发送、已接收数据进行反馈处理，可对数据进行状态变更，状态反馈回复
触发组件
接收交换数据
用于接收交换数据并执行自定义业务动作
二、
使用说明
发送交换数据
功能介绍：
发送交换数据是指数据发送方，进行交换数据发送时所需要用到的组件，涉及需要跨租户数据传输场景时，各租户之间通过数据交换中心连接传输，数据发送方连通交换中心后，通过自行配置的动作流中，配置发送交换数据执行组件，进行跨租户发送交换数据。
使用案例：
想要在不同租户间进行表单字段值、附件交换，发送方如何实现数据发送？
必要条件：
三、
必须采购数据交换中心、公文管理、e-builder、动作流、开放平台等
四、
开放平台下新建应用，用于中开放平台设置
五、
公文管理-交换平台设置测试通过并保存
操作步骤：
1.
添加触发事件
2.
添加发送交换数据组件
发送方填写对应业务数据，并选择接收单位，根据设置的ESB转换规则，对业务数据进行转换处理，然后调用平台的数据接口，把数据发送至平台
2.1交换平台团队、发送单位
根据接入系统交换平台设置中配置的平台信息自动带出
2.2数据标题：进行交换的数据标题设置，可输入固定值，也可通过动态赋值上下文选择内容赋值
2.3接收单位：可输入固定值，也可通过动态赋值上下文选择内容赋值
选择固定值时支持选择平台同步到接入系统中的平台单位
2.4选择交换模板：选择平台数据交换中心下新建的交换模板
交换模板选择范围为数据交换中心-交换模板设置下新建的数据交换模板
2.5交换模板数据：可输入固定值，也可通过动态赋值上下文选择内容赋值（至少设置一个交换模板数据对应关系）
反馈信息
功能介绍：
业务方B通过反馈执行组件可以将后续的事项的处理动态反馈给业务方A，收到业务方B反馈信息后业务方A也可以对B反馈的内容进行回复，从而形成双向反馈机制，提升协作效率。
反馈方为发送方
当操作方选择“发送方”时，显示接收单位、数据标识类型、发送数据标识、反馈类型、变更状态、 意见、回复内容等设置项
数据标识类型：支持发送数据标识（默认选中）、数据唯一标识；
发送数据标识：数据发送成功后，在数据交换中心生成的发送方数据的唯一标识。可通过【发送交换数据】执行组件的响应数据获取，即发送数据id；
数据唯一标识：业务发送方数据的唯一标识（不可重复），各业务方通过该标识对数据进行反馈或回复；
当选择“发送数据标识”后，下方展示【发送数据标识】字段，仅支持动态赋值方式
当选择“数据唯一标识”后，下方展示【数据唯一标识】【选择交换模板】字段
数据唯一标识：仅支持动态赋值；
选择交换默认：支持动态赋值（默认选中）、固定值。
反馈类型：复选框，支持变更平台数据状态（默认勾选）、回复处理事项动态：
变更平台数据状态：支持对平台的发送数据进行状态变更
变更状态选项：撤销（默认选中）、作废；
撤销/变更意见：支持动态赋值（默认选中）、固定值（多行文本）；
回复处理事项：对接收方后续处理事项的动态进行回复，回复内容：支持动态赋值（默认选中）、固定值（多行文本）。
反馈方为接收方
当操作方选择“接收方”时，显示接收数据标识、反馈类型等设置项
接收数据标识：数据接收成功后，在数据交换中心生成的接收方数据的唯一标识。可通过【接收交换数据】触发组件的参数列表获取，即接收数据id。仅支持动态赋值。
反馈类型：复选框，支持变更平台数据状态（默认勾选）、回复处理事项动态：
变更平台数据状态：支持对平台的发送数据进行状态变更
变更状态选项：签收（默认选中）、拒收、退回；
当选中拒收/退回时，下方需要填写拒收/退回意见，支持动态赋值、固定值（多行文本）两种方式，默认选中“动态赋值”
反馈处理事项：对已接收后续处理事项的动态进行反馈
反馈状态：支持动态赋值（默认选中）、固定值（单行文本）；
反馈内容：支持动态赋值（默认选中）、固定值（多行文本）。
使用案例：
在数据交换过程中，作为数据发送方获取到接收方事项处理反馈内容后想要对已发送的数据进行【作废】操作并回复接收方
必要条件：
①　必须采购数据交换中心、公文管理、e-builder、动作流、开放平台等
②　开放平台下新建应用，用于中开放平台设置
③　公文管理-交换平台设置测试通过并保存
操作步骤：
1. 添加触发事件
如配置eb事件触发-点击按钮时触发事件，配置触发时机点击按钮执行后触发
2. 添加反馈信息组件
用于发送方变更平台数据状态、对接收方反馈内容进行回复后续处理事项的动态。
2.1交换平台团队、发送单位
根据接入系统交换平台设置中配置的平台信息自动带出
2.2  数据标识类型：选择发送数据标识（默认选中）或数据唯一标识；
（1） 发送数据标识：数据发送成功后，在数据交换中心生成的发送方数据的唯一标识。可通过【发送交换数据】执行组件的响应数据获取，即发送数据id；
（2） 数据唯一标识：业务发送方数据的唯一标识（不可重复），各业务方通过该标识对数据进行反馈或回复；
2.3反馈类型：
（1） 变更平台数据状态：支持对平台的发送数据进行状态变更
a) 变更状态选项：选择作废；
b) 作废意见：根据实际场景填写，支持动态赋值（默认选中）、固定值（多行文本）；
（2） 回复处理事项：对接收方后续处理事项的动态反馈进行回复并填写回复内容。
效果展示：
发送方：反馈组件执行后，数据交换的数据状态变为【作废】且进行回复内容记录：
数据状态变更为【作废】：
回复处理事项动态记录：
数据交换平台数据状态变更为【作废】：
接收方：数据状态变更为作废并显示处理意见
接收交换数据
功能介绍：
接收交换数据是指数据接收方，进行交换数据接收时所需要用到的组件，涉及需要跨租户数据传输场景时，数据接收方连接数据交换中心后，在动作流中配置接收交换数据执行组件，进行跨租户接收交换数据。
使用案例：
想要获取到其他租户交换过来的表单字段值、附件，接收方如何实现数据接收？
必要条件：
六、
必须采购数据交换中心、公文管理、e-builder、动作流、开放平台等
七、
开放平台下新建应用，用于③中开放平台设置
八、
公文管理-交换平台设置测试通过并保存
操作步骤：
1.
添加接收交换数据组件
2.
选择交换模板
交换模板选择范围为数据交换中心-交换模板设置下新建的数据交换模板；接收交换数据组件中模板选择必须与发送方发送交换组件中选择的模板保持一致
3.
根据场景需求自行配置后续触发操作