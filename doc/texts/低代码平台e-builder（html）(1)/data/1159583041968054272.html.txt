一、功能介绍
条件判断组件用于判断某些动作是否执行/跳过。当满足条件时，条件判断内的动作会依次执行；当不满足条件时，则会跳过该条件判断内的所有动作，直接执行后续的动作。
如下图所示，由于未满足条件，条件判断下的动作直接跳过不执行：
二、使用说明
首先设置触发事件，之后添加条件判断组件并设置条件
在条件设置下方点击加号，添加其他组件，这些组件都包含在条件判断的虚线框中：
三、场景案例
判断合同金额决定是否提醒
场景案例：
合同审批流程离开发起节点后，触发动作流，判断当合同金额≥10万时，发送消息提醒财务负责人关注该笔合同订单，并且合同流程数据，新增到台账当中。
操作说明：
首先设置合同审批流程触发，触发时机选择离开发起节点时：
之后添加条件判断，设置合同金额≥10万，并在条件判断下方，添加消息提醒动作，设置提醒内容：
最后在条件判断框线的下方，添加新建表单数据，将合同流程转化成表单的台账数据：