新增表单迁移将其EB应用下的动作流同步迁移的功能
在应用A中存在表单A，在该应用的动作流tab页中建的动作流：
1、建的动作流和当前应用的任何一个表单都没关系，也就是动作流没有对应的表单组件，此时该动作流可通过关联应用按钮去解除该应用A与该动作流的关系
2、建的动作流存在该应用A下的表单A的组件，当表单A转移到应用B下时，该动作流会一起转移到应用B下，应用A下不显示该动作流
3、在A应用下建的动作流有表单组件，但表单组件选择的表单是应用B的，此时要解除该动作流和A应用的关系，可通过关联应用按钮去解除与应用A的关系