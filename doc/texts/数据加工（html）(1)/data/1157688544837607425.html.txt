1.数据源
选择数据需要输出的连接。若在数据来源中如果选择的输入方式是全量同步，此处可选择的数据连接类型包含mysql、Oracle、SqlServer、达梦、doris、hive 。如选择的输入方式为全量+增量同步，则此处只能选择mysql连接。
2.数据库
根据选择的连接，选择需要抽取表的数据库以及schema。部分数据库没有schema的概念，可以不做选择，例如mysql。
3.目标表
输出表的方式有两种，自动创建和手动选择。
自动创建：根据字段和库表信息，自动创建对应的表。
手动选择：根据数据库信息，选择一张已经存在的表。
4.清理SQL
可填入SQL语句。例如update xxx