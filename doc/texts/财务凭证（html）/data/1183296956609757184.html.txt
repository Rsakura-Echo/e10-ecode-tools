一、功能背景及价值
即凭证中间表，用于存储流程数据或者第三方数据，将这些数据写入凭证中间表，再配合引擎规则将中间表推到凭证查询台账eb
二、功能详细说明
进入【凭证业务数据】
业务数据由基础信息、主表信息、分录组成
基础信息：记录中间表数据id、数据生成日期、上游系统标识等
主表信息：记录会计凭证主表字段取值，例如账套、会计期间等
分录：记录会计凭证借贷分录取值，例如核算科目、摘要等
三、上下游关联功能点
如何将上游数据写入中间表